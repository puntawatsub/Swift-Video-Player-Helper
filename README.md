# Implementation

```swift
import SwiftUI
import AVKit

struct ContentView: View {
  var body: some View {
    VideoPlayer(player: playVideo(fileName: "filename", fileFormat: "fileformat"))
  }
}
```
